/target
# D2 打包产物（scripts/package-windows.sh / cargo packager）
/dist
# A1 工具链阶梯验证使用的独立构建目录（CARGO_TARGET_DIR=target-verify）
/target-verify
/docs/target-verify
# 本地代码图谱缓存（此前仅由 .git/info/exclude 覆盖，不随仓库分发）
/.codegraph

# Agent 本地运行证据与执行状态；正式计划仍纳入版本控制
/.omo/evidence/
/.omo/boulder.json
/.omo/start-work/
